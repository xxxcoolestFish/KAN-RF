# KAN-RF: 基于 KAN 可微世界模型的模型预测控制

## 1. 核心思路

用 **KAN（Kolmogorov-Arnold Network）** 构建可微分世界模型，实现**一个网络、两种用途**：

$$
\text{训练时：} \quad f_\theta(s, a) \rightarrow s' \quad \text{(前向预测)}
$$
$$
\text{部署时：} \quad a^* = \arg\min_a \|f_{\theta^*}(s, a) - s^*\|^2 \quad \text{(梯度优化反推动作)}
$$

训练阶段用标准的监督学习拟合动力学数据。部署时**冻结模型参数**，通过梯度下降在动作空间中寻找使预测状态最接近目标状态的动作。不需要单独的策略网络。

### 为什么用 KAN 而非 MLP

KAN 的每条边是显式的 1D B-样条函数：$\phi(x) = w \cdot \text{SiLU}(x) + \sum_k c_k B_k(x)$。这带来了三个 MLP 不具备的能力：

| 性质 | 机制 | 用途 |
|------|------|------|
| **局部支撑** | $B_k(x) > 0$ 仅在 $[t_k, t_{k+d+1}]$ 区间 | 在线学习时天然抗灾难性遗忘 |
| **分析导数** | $f'(x) = \frac{1}{h}\sum(\Delta c_i)B_i^{k-1}(x)$ | 精确的梯度信息用于动作优化 |
| **可认证界限** | $\|f^{(d)}\|_\infty \le h^{-d}\cdot\max\|\Delta^d c\|$ | 不需要 ensemble 即可量化模型不确定性 |

---

## 2. 核心挑战：前向-逆向差距（Forward-Inverse Gap）

### 问题描述

训练时，世界模型在**给定 (s, a)** 的数据点上最小化前向预测误差，训练后 RMSE ≈ 0.04（约 0.14 rad 的角度误差）。但在部署时，优化器在动作空间中**自由搜索**，依赖模型的局部几何（Jacobian $\partial f / \partial a$）来指导搜索方向。

**经验发现**：前向预测准确（MSE = 0.0018）时，逆向恢复的动作误差仍高达 0.87 N·m（力矩范围 ±2.0 N·m 的 44%）。只有 36.5% 的恢复落在 0.2 N·m 以内。

### 三个根因

**根因一：Jacobian 不匹配（可训练的差距）**

MSE 损失 $\mathcal{L} = \|f_\theta(s,a) - s'\|^2$ 的梯度中**不包含** $\partial f_\theta / \partial a$ 的项：
$$\frac{\partial \mathcal{L}}{\partial \theta} = 2(f_\theta - s')^T \cdot \frac{\partial f_\theta}{\partial \theta}$$

训练信号从不直接惩罚错误的 Jacobian。一个模型可以任意低的 MSE 达到同时拥有完全错误的导数。实测：训练后模型的 Jacobian 与真实 Jacobian 的余弦相似度仅为 **0.099**（≈84° 偏差）。

直觉类比：$g_{\text{true}}(a) = 0.5a$，但模型学了 $g_\theta(a) = 0.5a + 0.05\sin(15a)$。函数值误差 ≤ 0.05，但导数为 $0.5 + 0.75\cos(15a)$，在 -0.25 到 1.25 之间振荡——经常指向**相反方向**。

**根因二：残差偏移（可减少但无法消除的差距）**

即使 Jacobian 完美匹配（$D \ge 1$），前向残差 $\varepsilon_0 = f_\theta(s, a_{\text{true}}) - f_{\text{true}}(s, a_{\text{true}}) \neq 0$ 也会将逆目标的最小值从 $a_{\text{true}}$ 移开：
$$a^* = a_{\text{true}} - \frac{J^T \varepsilon_0}{\|J\|^2}$$

对于有限数据和模型容量，$\varepsilon_0$ 永远不可能精确为零。

**根因三：欠驱动放大效应（结构性的、不可消除的差距）**

Pendulum 有 2 个自由度（角度、角速度），但只有 1 个控制输入（力矩）。Jacobian 的范数 $\|J\| \approx 0.04$ 非常小。前向误差被放大：
$$\mathbb{E}[|a^* - a_{\text{true}}|] \approx \frac{\|\varepsilon\|}{\|J\|} \approx \frac{0.037}{0.0404} \approx 0.92$$

放大因子 ≈ 25。这说明即使前向误差为 0.037，逆向动作误差仍约为 0.92（归一化单位）。**这是物理系统的结构性约束，不是模型能解决的**。

---

## 3. 解决方案一：拟合深度训练（Fitting Depth）

### 核心概念

定义**拟合深度 D** 为模型导数匹配真实导数的最高阶数：
$$D = \max\left\{d \;\middle|\; \left\|\frac{\partial^k f_\theta}{\partial a^k} - \frac{\partial^k f_{\text{true}}}{\partial a^k}\right\| \le \epsilon_k,\; \forall k \in [0, d]\right\}$$

标准 MSE 训练只能达到 $D=0$。梯度下降优化 $a$ 时跟随的是 $\partial f_\theta/\partial a$ 而非 $f_\theta$ 本身，因此 $D \ge 1$ 是关键目标。

### 三级训练框架

**Level 1: MOPS（Multi-Order P-Spline）**——参数空间正则化

直接惩罚 B-样条控制点的二阶差分：
$$\mathcal{L}_{\text{MOPS}} = \lambda \cdot \sum_{\text{所有边}} \|\Delta^2 c\|^2$$

利用 P-spline 恒等式 $\|\Delta^d c\|^2 \approx h^{2d-1} \int [f^{(d-1)}(x)]^2 dx$，控制二阶差分等价于惩罚一阶导数能量。仅作用于 KAN 的 B-样条控制点，不需要真实 Jacobian。

**Level 2: CPDM（Critical-Path Derivative Matching）**——边空间匹配

识别对 Jacobian 贡献最大的关键边，在这些边上单独做导数匹配。

**Level 3: CWS（Controllability-Weighted Sobolev）**——输出空间匹配

当真实 Jacobian 可用时（解析动力学），直接惩罚模型 Jacobian 与真实 Jacobian 的偏差：
$$\mathcal{L}_{\text{CWS}} = \nu \cdot \|w \odot (\partial f_\theta/\partial a - J_{\text{true}})\|^2$$

可操控性加权：直接受控维度（如 $\dot{\theta}$）获得更高的权重。

### 实验结果（Pendulum 世界模型）

| 方法 | Forward MSE | Jacobian cos_sim | ‖Δ²c‖ | 逆误差均值 |
|------|:---:|:---:|:---:|:---:|
| Baseline (MSE only) | 0.001785 | 0.099 | 0.0986 | 0.507 |
| MOPS λ=0.1 | 0.001907 | 0.237 | 0.0208 | 0.428 |
| CWS ν=1.0 | 0.000337 | **0.979** | 0.1020 | 0.291 |
| **Hybrid** | **0.000198** | 0.924 | **0.0007** | **0.228** |

**关键发现**：MOPS 和 CWS 通过不同机制起作用——MOPS 降低控制点粗糙度（141×）但不改善方向，CWS 改善 Jacobian 方向（0.099→0.979）但不改善光滑度。两者互补，Hybrid 在所有指标上最优。逆误差从 1.01 N·m 降至 0.46 N·m（降 55%），但残余误差验证了根因三（约 0.46 N·m 的结构性下限）。

---

## 4. 解决方案二：多时间尺度世界模型

### 动机

根因三指出单步 Jacobian 太小（$\|J\| \approx 0.04$）。**但如果预测的是一段较长时间后的状态，动作对状态的影响会更大。**

### 方法

将世界模型从 $f(s, a) \rightarrow s_{t+dt}$ 扩展为 $f(s, a, k) \rightarrow s_{t+k\cdot dt}$，$k \in \{1, 2, 4, 8, 16\}$。输入多一维 $k/16$ 表示时间尺度。

### 架构

- 世界模型：KAN([5, 16, 3])，1152 参数
- 训练数据：对每个 (s, a)，用解析动力学生成 5 个时间尺度的 $s_{t+k\cdot dt}$
- 训练方法：MOPS（λ=0.1）

### 决策网络（Plan A）

将逆优化的结果蒸馏为一个快速的前馈决策网络：
$$(s, s^*) \xrightarrow{\text{KAN([6,12,2])}} (a, k)$$

决策网络同时输出动作和时间尺度。训练标签通过逆优化生成（对每个状态，尝试所有 k，选最佳 (a, k)）。

**结果：9/10 Pendulum swing-up 成功。** k=16 被选中 75% 的时间，验证了大时间尺度的必要性。

---

## 5. 核心难题：k-选择（k-Selection）

### k 的困境

多尺度世界模型引入了新的问题：在每个状态下，应该选哪个 k？

- **k 太小**：Jacobian 太小，前向误差被放大（根因三）
- **k 太大**：预测误差累积。混沌系统（如 Acrobot）中误差指数增长
- **需要状态相关的 k**：Pendulum 底部用 k=16（需要大 Jacobian），顶部用 k=1（精细控制）

### 尝试过的方法

**方法 1：世界模型自评（失败）**

思路：让世界模型自己评估"哪个 k 的预测结果最好"，选预测中最接近目标的 k。

失败原因：**循环推理**。世界模型在大 k 时预测更大的状态变化（更乐观），但预测更不准。这导致偏向大 k。Acrobot 因此选了 k=8（0% 成功率），而正确的 k=1 是 92%。

**方法 2：CKS——可认证 k-选择（理论正确，工程失败）**

思路：利用 B-样条导数上界 $\|f^{(d)}\|_\infty \le h^{-d} \cdot \max|\Delta^d c|$，分析性地计算每个 k 的预测误差上界：
$$k_{\text{cert}}(s) = \max\{k : E_k(s)/G(s,k) \le \varepsilon\}$$

其中 $E_k(s)$ 是 k 步预测误差上界，$G(s,k)$ 是 Jacobian 增益。

建立了完整的 6-定理理论框架，包括 PAC 式保证和四个系统类型的自动适配。

失败原因：多尺度世界模型在 k=1 的基础预测误差已经太高（L2≈0.38），导致对所有 k 的认证上界都太松，无法区分。

**方法 3：不确定性加权 MPC（区分力不够）**

思路：用 B-样条激活密度 $\rho(s)$ 作为不确定性代理，加权 MPC 评分。问题：训练数据密度均匀，$\rho(s)$ 跨状态变化太小，加上混沌系统的 Jacobian 补偿使惩罚项归零。

---

## 6. 解决方案三：动作探索器 + 决策网络微调（最终 10/10 方案）

### 思路

不再试图完美解决 k-选择。让系统在卡住时直接在环境中尝试不同动作，将成功经验蒸馏回决策网络。

### 三个全通用机制

**1. 检测卡住**：角度误差连续 N 步不下降 → 当前动作方向可能错误。

**2. 原地尝试候选动作**：
- 保存环境状态 `env.unwrapped.state`
- 在同一状态下尝试多个候选动作（模型建议的、反方向的、随机的）
- 用真实环境反馈衡量改善程度（不依赖世界模型预测）
- 恢复状态，选最优候选

**3. 记录纠正标签**：$(s, a_{\text{wrong}}) \rightarrow (s, a_{\text{correct}})$。用积累的纠正标签微调决策网络。

### 结果

**Pendulum-v1: 10/10 全通过。** 之前失败的所有 Trial（2, 3, 6）全部修复。这是目前 Pendulum 上的最优方案。

---

## 7. 解决方案四：世界模型 + 价值网络（WM+V，当前方向）

### 动机

k-选择的所有麻烦源于一个根本矛盾：**让世界模型同时承担"预测"和"看远"两项任务。** 新方案将两项职责分离：

| 组件 | 职责 | 训练方式 |
|------|------|------|
| 世界模型 f(s,a) | 单步预测 s'（最擅长的） | 离线 MOPS |
| 价值网络 V(s) | 估计累积未来奖励（看远） | 在线 TD(0) |

世界模型永远只用 k=1（预测最准）。V(s) 通过 TD 学习从数据中隐式编码"从 s 出发多久能到目标"。k 不再需要。

### 架构

- 世界模型：KAN([4, 12, 3])，冻结
- 价值网络：MLP([3, 32, 1])，~200 参数，极小
- 控制循环：MPC 评分 $\text{score}(a) = R(s, a, s') + \gamma \cdot V(s')$
- 更新：TD(0)，小 replay buffer（500 transitions）

与 AlphaGo 的 MCTS + value network 同构——世界模型替代 MCTS rollout，V(s) 替代 value network。

### 核心问题：冷启动鸡-蛋死锁

V(s) 初始化为接近零的随机值，所有状态 V≈0。MPC 退化为贪婪策略——底部摆锤需要 V 提供远见，但 V 需要好的轨迹来学习。**鸡-蛋死锁**。

初始结果：22/30 (73%)。ε-greedy 探索效率低（从底部随机探索到顶部几乎不可能）。

### 尝试：离线预训练 V(s)

**思路**：用冻结的世界模型生成合成轨迹（从随机状态出发，逆优化找动作，世界模型预测下一步），计算 MC 回报 $G_t = \sum \gamma^{\tau-t} r_\tau$，回归训练 V(s)。

**实验**：

| 世界模型 | k | G 范围 | 15 轮成功率 | 30 轮成功率 |
|------|:---:|------|:---:|:---:|
| 冷启动 | — | — | 67% | 73% |
| 单尺度 [4,12,3] | 1 | [-90.6, 0] | 73% | — |
| 多尺度 [5,16,3] | 16 | [-43.0, 0] | 80% | — |
| **多尺度 [5,16,3]** | **4** | **[-69.7, 0]** | **87%** | **77%** |

**关键发现**：
1. 单尺度 k=1 做 30 步自回归，累积误差大，G 过于悲观（-90.6），V(s) 质量差
2. 多尺度 k=16 做 2 步自回归，但恒力矩 0.8 秒太粗糙，目标态会被错误估值
3. **k=4 是最佳平衡**：Jacobian ~4×，0.2 秒粒度适中
4. 预训练主要改善早期性能（15 轮：67%→87%），长期差距缩小（30 轮：73%→77%）
5. **77% 仍远低于动作探索器的 100%**——WM+V 的结构性天花板：单步贪婪 MPC 无法做多步协调规划

---

## 8. 四个环境完整结果

| 环境 | 世界模型 | 控制方法 | 成功率 | 关键洞察 |
|------|------|------|:---:|------|
| Pendulum-v1 | [5,16,3] 多尺度 | 决策网络 + 动作探索器 | **100%** | 原地尝试真实环境反馈是关键 |
| Pendulum-v1 | [4,12,3] 单尺度 | WM+V (k=4 预训练) | 77% | 预训练改善早期，天花板受限于贪婪 MPC |
| CartPole-v1 | [7,20,4] 多尺度 | MPC k=2 + 能量评分 | **99%** | 失败是小车漂移，非杆子倒下 |
| MountainCar-v0 | [6,16,2] 多尺度 | MPC k=4 + 能量评分 | **100%** | 评分函数从"最大化位置"改为"最大化能量"是关键 |
| Acrobot-v1 | [10,24,6] 多尺度 | MPC k=1 | **92%** | 混沌系统，k=8 时误差爆炸 |

---

## 9. 关键洞察总结

### 已确认的

1. **前向 MSE 好 ≠ 反推动作准**。根因三（欠驱动放大）是结构性的物理约束，即使模型完美也存在。
2. **多尺度世界模型部分解决根因三**。k=16 的 Jacobian 比 k=1 大 16 倍，但代价是引入了 k-选择问题。
3. **"世界模型自我评估"是死路**。模型高估自己在长 horizon 的预测能力，导致循环推理。
4. **动作探索器是最强的方案**。直接在真实环境中尝试候选动作，不依赖模型预测未来——没有模型误差放大问题。
5. **B-样条局部支撑是 KAN 的核心优势**。这是本项目与其他 MBRL 工作的本质区别。
6. **离线预训练 V(s) 可行但不充分**。能加速收敛，但无法突破贪婪 MPC 的结构性天花板。
7. **CKS 理论正确但工程上暂时不可行**。基础预测误差太高导致认证上界太松，需要更高精度的模型。

### 未解决的核心问题

**如何让模型"看远"而不累积预测误差？** 这是贯穿所有尝试的主线问题。多尺度模型用 k 参数化了这个问题，WM+V 用 V(s) 隐式编码长 horizon 效果，但两者都未根本解决。动作探索器绕过了这个问题（直接问真实环境），但代价是需要环境交互。

---

---

*文档更新于 2026-05-28。*
